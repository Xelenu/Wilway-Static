name: Combine Traffic Tour Data Parts

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  combine_data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Combine WebglBuild.data.unityweb parts
        run: |
          cat ./core/gam-scripts/traffic-tour/Build/WebglBuild.data.unityweb.000.part \
              ./core/gam-scripts/traffic-tour/Build/WebglBuild.data.unityweb.001.part \
              ./core/gam-scripts/traffic-tour/Build/WebglBuild.data.unityweb.002.part \
            > ./core/gam-scripts/traffic-tour/Build/WebglBuild.data.unityweb

      - name: Commit combined data file
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add ./core/gam-scripts/traffic-tour/Build/WebglBuild.data.unityweb
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Combine WebglBuild.data.unityweb parts into single file"
            git push
          fi
