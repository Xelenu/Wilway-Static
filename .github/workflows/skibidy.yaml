name: Combine HTML5 WASM Parts

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  combine_wasm:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Combine html5.wasm parts
        run: |
          cat ./core/gam-scripts/skibidi-toilet-terrors/Build/html5.wasm.000.part \
              ./core/gam-scripts/skibidi-toilet-terrors/Build/html5.wasm.001.part \
            > ./core/gam-scripts/skibidi-toilet-terrors/Build/html5.wasm

      - name: Commit combined wasm file
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add ./core/gam-scripts/skibidi-toilet-terrors/Build/html5.wasm
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Combine html5.wasm parts into single file"
            git push
          fi
